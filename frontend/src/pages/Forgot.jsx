import {useState} from 'react';import {Link} from 'react-router-dom';import {api} from '../services/api';
export default function Forgot(){const [email,setEmail]=useState('');const [msg,setMsg]=useState('');return <div className="simple-auth"><form onSubmit={async e=>{e.preventDefault();try{const r=await api.forgot({email});setMsg(r.message)}catch{setMsg('Reset request recorded.')}}}><h1>Forgot Password</h1><p>Enter Your Registered Work Email.</p><label>Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><button className="btn full">Send Reset Instructions</button>{msg&&<div className="success">{msg}</div>}<Link to="/login">Back To Login</Link></form></div>}

import {useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import {ShieldCheck,Eye,EyeOff} from 'lucide-react';import Logo from '../components/Logo';import {useAuth} from '../context/AuthContext';
export default function Login(){const [f,setF]=useState({email:'admin@crosscheck.com',password:'Admin@123'});const [show,setShow]=useState(false);const [error,setError]=useState('');const [busy,setBusy]=useState(false);const {login}=useAuth();const nav=useNavigate();const submit=async e=>{e.preventDefault();setBusy(true);setError('');try{await login(f);nav('/app')}catch(x){setError('Invalid credentials or backend is not running.')}finally{setBusy(false)}};return <div className="auth-page"><div className="auth-brand"><Logo light/><div><span className="eyebrow">SECURE ROLE-BASED ACCESS</span><h1>Verify Smarter.<br/>Hire with Confidence.</h1><p>One platform for HR, Recruiters and Administrators to Manage Candidate Verification And Risk Intelligence.</p></div><div className="auth-points"><span><ShieldCheck/> Role-based Authorization</span><span><ShieldCheck/> Protected Hiring Recods</span><span><ShieldCheck/> Complete Audit Trail</span></div></div><div className="auth-panel"><form onSubmit={submit}><h2>Welcome back</h2><p>Sign in to your CrossCheck workspace.</p>{error&&<div className="error">{error}</div>}<label>Email<input value={f.email} onChange={e=>setF({...f,email:e.target.value})} type="email" required/></label><label>Password<div className="password"><input value={f.password} onChange={e=>setF({...f,password:e.target.value})} type={show?'text':'password'} required/><button type="button" onClick={()=>setShow(!show)}>{show?<EyeOff/>:<Eye/>}</button></div></label><div className="form-row"><span></span><Link to="/forgot-password">Forgot password?</Link></div><button className="btn full" disabled={busy}>{busy?'Signing in…':'Login'}</button><div className="auth-switch">New to CrossCheck? <Link to="/register">Create an account</Link></div><div className="demo-box"><b>Demo accounts</b><small>Admin: admin@crosscheck.com / Admin@123</small><small>HR: hr@crosscheck.com / Hr@123</small><small>Recruiter: recruiter@crosscheck.com / Recruiter@123</small></div><Link className="back" to="/">← Back to home</Link></form></div></div>}
